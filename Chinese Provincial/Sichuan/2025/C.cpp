#include <bits/stdc++.h>
typedef long long ll;
#define PII pair<ll, ll>
using namespace std;

const ll N = 1e5 + 10;

vector<ll> factor(ll x)
{
    vector<ll> ret;
    for (ll i = 2; i * i <= x; i++)
    {
        if (x % i == 0)
        {
            ret.push_back(i);
            if ()
        }
    }
}

void solve()
{
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}
/*
1
300 4
1 2 1 1
2 3 1 1
3 2 1 1
3 300 1 1

*/
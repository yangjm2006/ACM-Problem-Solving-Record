#include <bits/stdc++.h>

using namespace std;

const int N = 1e6;
typedef long long ll;

void __() {
	ll x1, x2, y1, a, b, y2;
	cin >> x1 >> x2 >> y1 >> a >> b >> y2;
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	__();
	return 0;
}